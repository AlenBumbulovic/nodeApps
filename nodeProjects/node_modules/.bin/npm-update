#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../npm-update/bin/npm-update" "$@"
  ret=$?
else 
  node  "$basedir/../npm-update/bin/npm-update" "$@"
  ret=$?
fi
exit $ret
